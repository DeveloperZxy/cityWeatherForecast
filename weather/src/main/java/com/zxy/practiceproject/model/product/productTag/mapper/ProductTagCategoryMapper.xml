<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zxy.practiceproject.model.product.productTag.mapper.ProductTagCategoryMapper">

    <select id="pageQuery" resultType="com.zxy.practiceproject.model.product.productTag.pojo.dto.PageProductTagCategoryDTO">
        SELECT *
        FROM product_tag_category
        <where>
            <if test="filters!=null">
                <if test="filters.name != null and filters.name != ''">
                    AND product_tag_category.name like concat('%',#{filters.name},'%')
                </if>
                <if test="filters.description != null and filters.description != ''">
                    AND product_tag_category.description like concat('%',#{filters.description},'%')
                </if>
                <if test="filters.creationTimeStart != null">
                    and article.create_time <![CDATA[>=]]> #{filters.creationTimeStart}
                </if>
                <if test="filters.creationTimeEnd != null ">
                    and article.create_time <![CDATA[<=]]> #{filters.creationTimeEnd}
                </if>
                <if test="filters.status != null and filters.status != ''">
                    AND article.status= #{filters.status}
                </if>
                <if test="filters.code != null and filters.code != ''">
                    AND article.code LIKE CONCAT('%', #{filters.code}, '%')
                </if>
            </if>
            AND product_tag_category.deleted_status =0
        </where>
    </select>
    <select id="listQuery" resultType="com.zxy.practiceproject.model.product.productTag.pojo.entity.ProductTagCategoryEntity"
            parameterType="com.zxy.practiceproject.model.product.productTag.pojo.dto.QueryProductTagCategoryDTO">
        SELECT *
        FROM product_tag_category
        <where>
            <if test="filters!=null">
                <if test="filters.name != null and filters.name != ''">
                    AND product_tag_category.name like concat('%',#{filters.name},'%')
                </if>

                <!--                <if test="filters.includeRoot != null and filters.includeRoot == '1'">-->
                <!--                    AND product_tag_category.id != '0'-->
                <!--                </if>-->
                <!--                <if test="filters.parentId != null and filters.parentId != ''">-->
                <!--                    and JSON_CONTAINS(product_tag_category.parent_ids,json_array(#{filters.parentId}))-->
                <!--                </if>-->
                <!--                <if test="filters.keyword != null and filters.keyword != ''">-->
                <!--                    AND (-->
                <!--                    product_tag_category.name like concat("%", #{filters.keyword}, "%")-->
                <!--                    )-->
                <!--                </if>-->

            </if>
            AND product_tag_category.deleted_status =0
        </where>
    </select>
    <select id="listChildrenIdsAndSelf" resultType="java.lang.String" parameterType="java.lang.String">
        SELECT id
        FROM product_tag_category
        <where>
            JSON_CONTAINS(parent_ids, json_array(#{id}))
            AND product_tag_category.deleted_status =0
        </where> 
           OR id = #{id}
    </select>

</mapper>